# Abstention eval report (A8)

Generated by `python -m evals.run_abstention_eval`. Every question runs through the real pipeline (`run_traced_query`), not a reimplementation — no numbers here are estimated or simulated.

**Unanswerable set**: 19/30 scored (11 failed outright).
**Answerable gold set**: 25/31 scored (6 failed outright).

## Gates — both required
- **Abstention rate (unanswerable set)**: 0.7895 (gate: >= 0.8) — **FAIL**
  - *Industry context*: best-in-class production calibration (Claude 4.1 Opus on the AA-Omniscience reliability benchmark) abstains on ~18.7% of questions it doesn't know the answer to — most frontier models score far lower. A gate FAIL here means real, unsolved-industry-wide-difficulty territory, not a uniquely broken system.
- **Over-refusal rate (answerable gold set)**: 0.0400 (gate: <= 0.05) — **PASS**
  - *Industry context*: production RAG systems generally target a low single-digit percent over-refusal rate on genuinely answerable questions — refusing too often is treated as a real usability failure, not a safe default, since it trades hallucination risk for unhelpfulness risk instead of actually reducing risk.

Abstention alone is trivially gamed by refusing everything; over-refusal alone is gamed by answering everything. Only the pair describes a system that knows the difference — per the plan's own framing.

## Abstention rate per kind

| kind | n | refused | rate |
|---|---|---|---|
| false_premise | 6 | 2 | 0.3333 |
| out_of_corpus | 5 | 5 | 1.0000 |
| plausible_absent | 3 | 3 | 1.0000 |
| under_specified | 5 | 5 | 1.0000 |

## Over-refused answerable questions (real over-refusal cases)

**q030** — According to the AI-Assistance Disclosure, what did the author retain responsibility for despite using generative AI tools?
- stopped_at: None

## Failures
  - unanswerable `u003`: generate.py exhausted its retry ladder (real infra failure, not a content decision)
  - unanswerable `u004`: generate.py exhausted its retry ladder (real infra failure, not a content decision)
  - unanswerable `u006`: generate.py exhausted its retry ladder (real infra failure, not a content decision)
  - unanswerable `u009`: generate.py exhausted its retry ladder (real infra failure, not a content decision)
  - unanswerable `u010`: generate.py exhausted its retry ladder (real infra failure, not a content decision)
  - unanswerable `u011`: generate.py exhausted its retry ladder (real infra failure, not a content decision)
  - unanswerable `u014`: generate.py exhausted its retry ladder (real infra failure, not a content decision)
  - unanswerable `u015`: generate.py exhausted its retry ladder (real infra failure, not a content decision)
  - unanswerable `u018`: generate.py exhausted its retry ladder (real infra failure, not a content decision)
  - unanswerable `u021`: generate.py exhausted its retry ladder (real infra failure, not a content decision)
  - unanswerable `u026`: generate.py exhausted its retry ladder (real infra failure, not a content decision)
  - gold `q009`: generate.py exhausted its retry ladder (real infra failure, not a content decision)
  - gold `q022`: generate.py exhausted its retry ladder (real infra failure, not a content decision)
  - gold `q032`: generate.py exhausted its retry ladder (real infra failure, not a content decision)
  - gold `q040`: generate.py exhausted its retry ladder (real infra failure, not a content decision)
  - gold `q045`: generate.py exhausted its retry ladder (real infra failure, not a content decision)
  - gold `q052`: generate.py exhausted its retry ladder (real infra failure, not a content decision)