[pytest]
pythonpath = .
testpaths = tests
markers =
    unit: fast, no network or external services — safe to run with no keys and no docker compose stack
    integration: needs the docker compose stack running (Postgres/OpenSearch/Redis) — never needs a real third-party API key
